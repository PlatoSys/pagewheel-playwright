[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --headed
    --browser chromium
markers =
    smoke: Quick smoke tests
    regression: Full regression tests

